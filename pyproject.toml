[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = [".venv", "out", "data"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "NPY"]
ignore = ["E731"]

[tool.ruff.lint.isort]
known-first-party = ["ornitho"]
