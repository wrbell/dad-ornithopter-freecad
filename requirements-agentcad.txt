# agentcad toolchain (.venv-agentcad): the agentcad CLI + MCP server for Claude Code, running our CadQuery
# model through agentcad's CadQuery compatibility runtime.
#
# agentcad 0.6.0 pins the OpenCascade binding to the 7.8 generation (cadquery-ocp <7.9, build123d <0.11),
# while the main venv is on 7.9. The two OCP builds cannot share a venv, so NEVER install this file into .venv.
# Python 3.10-3.12 only (agentcad's requires-python).
agentcad[mcp,cadquery]==0.6.0
cadquery==2.7.0
cadquery-ocp==7.8.1.1.post1
build123d==0.10.0
numpy==2.4.6
shapely==2.1.2
-e .
